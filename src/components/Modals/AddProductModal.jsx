import Modal from "../Portals/Modal.jsx";

const AddProductModal = ({onOpen, onClose}) => {
    return (
        <Modal title="Add Products" onOpen={onOpen} onClose={onClose}>
            asdas
        </Modal>
    );
};

export default AddProductModal;