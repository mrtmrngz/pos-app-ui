import Modal from "../Portals/Modal.jsx";

const AddCategoryModal = ({onOpen, onClose}) => {
    return (
        <Modal title="Add Category" onOpen={onOpen} onClose={onClose}>
            asdas
        </Modal>
    );
};

export default AddCategoryModal;